# Implementation Plan

## Phase 1 — Foundation

- Confirm environment variables and apply the Supabase migration.
- Keep static JSON as the only project content source.
- Confirm all routes, static params and metadata build.

## Phase 2 — Visual fidelity

- Match the supplied desktop references section by section.
- Implement the responsive interpretation without flattening typography or visual hierarchy.
- Add GSAP ScrollTrigger choreography only after static layout is correct.

## Phase 3 — Contact workflow

- Exercise validation, honeypot, rate limit, database insert and optional email notification.
- Add structured operational logging without recording message bodies or raw IPs.

## Phase 4 — Quality gate

- Typecheck, lint, unit tests and production build.
- Keyboard, screen-reader, reduced-motion and device-width checks.
- Performance audit with representative project detail pages.
- Deploy and re-run the contact flow against production environment variables.
